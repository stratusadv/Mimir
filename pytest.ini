[pytest]
testpaths = app/tests
python_files = test_*.py
python_functions = test_*
addopts = -q --strict-markers
markers =
    ffmpeg: requires the ffmpeg executable on PATH
    windows: requires cmd.exe and therefore Windows
